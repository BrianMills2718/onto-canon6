profile:
  id: dodaf_minimal_strict
  version: "0.1.0"
  name: "DoDAF Minimal Strict"
  ontology_version: "dodaf_minimal@0.1.0"
  rule_version: "dodaf_minimal_rules@0.1.0"

ontology:
  source: custom
  mode: closed
  normalization_policy: conservative
  proposal_policy: reject
  pack_id: dodaf_minimal
  pack_version: "0.1.0"

validation:
  type_adapter: ontology_pack
  type_check_policy: exact

ingestion:
  allow_heuristic_default: false
