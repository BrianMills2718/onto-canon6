# tests/evaluation

Evaluation-focused tests for `onto-canon6`.

These tests should prove the extraction-quality baseline, evaluator behavior,
and acceptable-set semantics with deterministic fixtures where possible.

