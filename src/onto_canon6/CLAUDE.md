# src/onto_canon6

Primary Python package for `onto-canon6`.

Subtrees here should preserve the split between adapters, artifacts, core,
evaluation, pipeline, ontology runtime, and surfaces so the bootstrap remains
observable and debuggable.

